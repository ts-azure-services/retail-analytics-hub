properties:
  template:
    scale:
      minReplicas: 1
    containers:
      - name: agent2
        probes:
          - type: Liveness
            httpGet:
              path: /health
              port: 8002
              scheme: HTTP
          - type: Readiness
            httpGet:
              path: /health
              port: 8002
              scheme: HTTP
